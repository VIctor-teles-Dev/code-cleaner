services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: wbc
      POSTGRES_PASSWORD: wbc
      POSTGRES_DB: wbc
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wbc -d wbc"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend-api:
    build:
      context: ./apps/backend-api
    environment:
      PORT: 8080
      DATABASE_URL: postgres://wbc:wbc@postgres:5432/wbc?sslmode=disable
    ports:
      # host 8081 -> container 8080 (a 8080 do host está em uso por outro projeto)
      - "8081:8080"
    depends_on:
      postgres:
        condition: service_healthy

  web-app:
    build:
      context: .
      dockerfile: apps/web-app/Dockerfile
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      - backend-api

volumes:
  postgres-data:
